#  1. Variables in Python

Variables in Python are used to store data values. They are created when you assign a value to them, and you don’t need to declare their type (Python is dynamically typed).

### Syntax for Variable Assignment:

```python
x = 5  # Assigning an integer value to the variable x
y = "Hello"  # Assigning a string value to the variable y
```

### Variable Naming Rules:

- Variable names can contain letters (a-z, A-Z), numbers (0-9), and underscores (_).
- Variable names must start with a letter or an underscore.
- Variable names are case-sensitive (`Name` and `name` are different).

### Example:

```python
age = 25
name = "John"
is_student = True
```

```python
_abc=20  # it is a variable that has been assigned(stored) the value 20
print(_abc) #output: 20 because it is a variable that has been assigned(stored) the value 20
print("_abc") #output: _abc  because it is a string literal
```

```python
name ="Ruchitha"
Name="Preetham"
print(name) #This will print the value of the variable 'name' which is "Ruchitha".
print(Name) #Python is case sensitive language. So, name and Name are treated as two different variables.
```

# 2. Data Types in Python

Python has various built-in data types. Some common ones are:

- int: For integers (e.g., 1, -3, 100)
- float: For floating-point numbers (e.g., 3.14, -0.001)
- str: For strings (e.g., "Hello", "Python")
- bool: For boolean values (True or False)

### Type Checking:

You can use the `type()` function to check the type of a variable. 

```python
x = 10
print(type(x))  # Output: <class 'int'>
```

```python
age=22 # it is a variable that stores the age of a person
is_student=True # it is a variable that stores a boolean value indicating if the person is a student
weight=70.5 # it is a variable that stores the weight of a person
print("Age:", age) # it prints the age of the person
print("Is Student:", is_student) # it prints the boolean value indicating if the person is a student
print("Weight:", weight) # it prints the weight of the person
print(type(age)) # it prints the type of the variable 'age'
print(type(is_student)) # it prints the type of the variable 'is_student'
print(type(weight)) # it prints the type of the variable 'weight'
```

# 3. Type Conversion

Python allows you to convert between data types using functions like `int()`, `float()`, `str()`, etc.

### Example:

```python
x = "10"  # x is a string
y = int(x)  # Convert string to integer
z = float(y)  # Convert integer to float
print(z)  # Output: 10.0
```
```python
a=10 # a is an integer
print(a)# a is still an integer, output: 10
a=float(a) # a is now a float
print(a)# a is still a float, output: 10.0
```

# 4. Arithmetic Operators

Python supports basic arithmetic operations like addition, subtraction, multiplication, division, and more.

### Common Operators:

- `+` (Addition)
- `-` (Subtraction)
- `*` (Multiplication)
- `/` (Division)
- `//` (Floor Division)
- `%` (Modulus)
- `**` (Exponentiation)

### Examples:

```python
a = 10
b = 3
print(a + b)  # Output: 13
print(a - b)  # Output: 7
print(a * b)  # Output: 30
print(a / b)  # Output: 3.3333...
print(a // b)  # Output: 3 (Floor Division)
print(a % b)  # Output: 1 (Modulus)
print(a ** b)  # Output: 1000 (Exponentiation)
```

# 5. Assigning Values to Multiple Variables

Python allows you to assign values to multiple variables in a single line.

### Example:

```python
x, y, z = 10, 20, 30
print(x)  # Output: 10
print(y)  # Output: 20
print(z)  # Output: 30
```

You can also assign the same value to multiple variables in one line:

```python
x = y = z = 100
print(x, y, z)  # Output: 100 100 100
```

# 6. Variable Reassignment

You can change the value of a variable at any point in your program.

### Example:

```python
x = 5
print(x)  # Output: 5
x = 10
print(x)  # Output: 10
```

# HomeWork

- **Arithmetic Practice:** Write a Python program that performs basic arithmetic operations (addition, subtraction, multiplication, and division) on two numbers. Define the two numbers as variables within the code and print the results for each operation.

- **Swap Two Variables:** Write a Python program that swaps the values of two variables with and without using a third variable.

```python
# 1. Arithmetic Operations Program – Explanation
a = 20
b = 10
# a = 20 → Assigns the value 20 to variable a.
# b = 10 → Assigns the value 10 to variable b.
print("First Number =", a)
print("Second Number =", b)
# print() → Displays output on the screen.
# Shows the values stored in a and b.
print("Addition =", a + b)
# + → Addition operator.
#Adds a and b (20 + 10 = 30).
print("Subtraction =", a - b)
#- → Subtraction operator.
#Subtracts b from a (20 - 10 = 10).
print("Multiplication =", a * b)
#* → Multiplication operator.
#Multiplies a and b (20 × 10 = 200).
print("Division =", a / b)
#/ → Division operator.
#Divides a by b (20 ÷ 10 = 2.0).
```

```python
#2. Swap Using Third Variable – Explanation
a = 10
b = 20
#Assigns values 10 and 20 to variables a and b.
print("Before Swapping:")
print("a =", a)
print("b =", b)
#Displays the values before swapping.
temp = a
#Stores the value of a in a temporary variable temp.
#Now temp = 10.
a = b
#Copies the value of b into a.
#Now a = 20.
b = temp
#Copies the value stored in temp into b.
#Now b = 10.
print("After Swapping:")
print("a =", a)
print("b =", b)
#Displays the swapped values.

#Result:

#Before: a = 10, b = 20
#After: a = 20, b = 10
```

```python
#3. Swap Without Using Third Variable – Explanation
a = 10
b = 20
#Assigns values to variables a and b.
print("Before Swapping:")
print("a =", a)
print("b =", b)
#Displays values before swapping.
a, b = b, a
#Python swaps both values in a single statement.
#Value of b goes to a.
#Value of a goes to b.
print("After Swapping:")
print("a =", a)
print("b =", b)
#Displays the values after swapping.

#Result:

#Before: a = 10, b = 20
#After: a = 20, b = 10
```


