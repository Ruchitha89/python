# 2. String Manipulation

Strings are sequences of characters. Python provides many useful methods to manipulate strings.

### 2.1 Common String Operations:

- **Concatenation**: Joining two or more strings together using the `+` operator.

```python
first_name = "John"
last_name = "Doe"
full_name = first_name + " " + last_name
print(full_name)  # Output: "John Doe"
```

### Example:
```python
first_name="Ruchitha" #This is a variable that stores the first name of a person.
last_name="Shetty" #This is a variable that stores the last name of a person.
full_name=first_name+" "+last_name #This line concatenates the first name and last name with a space in between and stores it in the variable full_name.
print(full_name) #This line prints the full name of the person to the console.
```

- **Repetition**: Repeating a string multiple times using the `*` operator.

```python
greeting = "Hello! " * 3
print(greeting)  # Output: "Hello! Hello! Hello! "
```

```python
name="Ruchitha Shetty " * 100 # This will repeat the string "Ruchitha Shetty " 100 times and assign it to the variable 'name' and space will be added after the name as well.
print(name)# This will print the value of the variable 'name' which contains the repeated string.
```

```python
print("Hii Ruchitha" *4) # Output:Hii RuchithaHii RuchithaHii RuchithaHii Ruchitha
```

### 2.2 String Methods:

There are many string methods you can search in google.

- `upper()`: Converts a string to uppercase.
- `lower()`: Converts a string to lowercase.
- `strip()`: Removes leading and trailing spaces from a string.
- `replace(old, new)`: Replaces a substring with another string.

```python
message = "  Hello, World!  "
print(message.strip())  # Output: "Hello, World!"
print(message.upper())  # Output: "HELLO, WORLD!"
print(message.replace("World", "Python"))  # Output: "Hello, Python!"
```

### Example:

```python
a="Ruchitha Shetty " # string 
print(a.upper()) # converts the string to uppercase
print(a.lower()*5) # converts the string to lowercase and repeats it 5 times
print(a.split()*2) # splits the string into a list of words and represent it in double quotes and repeats the list 2 times
print(a.replace("Ruchitha","Ruchi")) # replaces the word "Ruchitha" with "Ruchi" in the string
```

```python
a="Hello 'Ruchitha Shetty'" #This is a string variable that contains the text "Hello 'Ruchitha Shetty'". The single quotes around Ruchitha Shetty are included in the string because the entire string is enclosed in double quotes. This allows us to include single quotes without needing to escape them.
print(a) # This line prints the value of the variable 'a' to the console. The output will be: Hello 'Ruchitha Shetty'
```

### 2.3 Length of String

```python
a="Hello 'Ruchitha Shetty'" #This is a string variable that contains the text "Hello 'Ruchitha Shetty'". The single quotes around Ruchitha Shetty are included in the string because the entire string is enclosed in double quotes. This allows us to include single quotes without needing to escape them.
print(len(a)) # This line prints the length of the string stored in variable 'a' to the console. The output will be: 21
```

### 2.4 Accessing String Characters:

You can access individual characters in a string using **indexing**. Python uses zero-based indexing, so the first character has an index of 0.

```python
text = "Python"
print(text[0])  # Output: P
print(text[2])  # Output: t
```

<img width="1202" height="632" alt="image" src="https://github.com/user-attachments/assets/e5fccb9c-82b3-4aa7-8e32-55bef04c27c4" />

### Example:

```python
a="Ruchitha" #string
print(a[1]) #prints the second character of the string output: u
```

You can also use **negative indexing** to start counting from the end of the string.

```python
print(text[-1])  # Output: n
print(text[-3])  # Output: h
```

### Example:

```python
a="Ruchitha" #string
print(a[-1]) #prints the last character of the string output: a because the index starts from 0 and -1 refers to the last character of the string.
```

### 2.5 Slicing Strings:

You can extract a portion (substring) of a string using slicing.

```python
text = "Python Programming"
print(text[0:6])  # Output: Python (extracts from index 0 to 5)
print(text[:6])  # Output: Python (same as above)
print(text[7:])  # Output: Programming (from index 7 to the end)
```

### Example:

```python
a="Ruchitha" #string
print(a[0:5]) 
'''Ruchi because it will print from 0 to 4 index and 5 is not included why because in python the index starts from 0 and it will print the characters at those index positions.
So, it will print characters at index 0, 1, 2, 3, and 4 which are 'R', 'u', 'c', 'h', and 'i' respectively.why 5 is not included because in python the end index is exclusive, meaning it will not include the character at the end index position.'''
b="chandan"
print(b[2:7]) #handan because it will print from 2 to 6 index and 7 is not included
```

### Example 2:

```python
a="chandan"# string slicing
print(a[2:])# it will print from 2nd index to end
print(a[:4])# it will print from 0 to 4th index it won't print 4th index
```

<img width="1232" height="635" alt="image" src="https://github.com/user-attachments/assets/42534ad3-6eef-40f5-bd03-b0838d913ede" />

```python
a="Ruchitha Shetty"# string slicing
print(a[0:7:2]) # output: Rcih because it starts from index 0 and goes up to index 7 with a step of 2, but dont include index 7
print(a[0:12:2]) # output: Rcih h because it starts from index 0 and goes up to index 12 with a step of 2, but dont include index 12 it consider the space as well
print(a[::2])# output: Rcih hty because it starts from index 0 and goes up to the end of the string with a step of 2
print(a[::3])# output: Rhhst because it starts from index 0 and goes up to the end of the string with a step of 3
```

# 4. Escape Sequences

Escape sequences are special characters in strings that start with a backslash (`\`). They are used to represent certain special characters.

Some commonly used escape sequences:

- `\n`: New line
- `\t`: Tab space
- `\\`: Backslash

```python
print("Hello\nWorld")  # Output: 
# Hello
# World

print("Hello\tPython")  # Output: Hello    Python
```

# Home Work

1. **Simple Greeting Program**: Write a Python program that asks the user for their name and age, then prints a personalized greeting message. Use both the `+` operator and `f-strings` for output.

**Example:**

```python
Enter your name: Alice
Enter your age: 25
Output: Hello, Alice! You are 25 years old.
```

2. **String Manipulation Exercise**: Write a Python program that:

- Takes a sentence as input from the user.
- Prints the sentence in all uppercase and lowercase.
- Replaces all spaces with underscores.
- Removes leading and trailing whitespace.

**Example:**

```python
Input: "   Python is awesome!   "
Output:
Uppercase: "PYTHON IS AWESOME!"
Lowercase: "python is awesome!"
Replaced: "___Python_is_awesome!___"
Stripped: "Python is awesome!"
```

3. **Character Counter**: Write a Python program that:

- Asks the user for a string.
- Prints how many characters are in the string, excluding spaces.

**Example:**

```python
Input: "Hello World"
Output: "Number of characters (excluding spaces): 10"
```

4. **Escape Sequence Practice:** Write a Python program that uses escape sequences to print the following output:

**Example:**

```python
Hello
    World
This is a backslash: \
```

### Solutons:

**1**

```python
# Asking the user to enter their name
name = input("Enter your name: ")

# Asking the user to enter their age
age = input("Enter your age: ")

# Using + operator
print("Hello, " + name + "! You are " + age + " years old.")

# Using f-string
print(f"Hello, {name}! You are {age} years old.")
```

**2**

```python
# Taking a sentence from the user
sentence = input("Enter a sentence: ")

# Printing in uppercase
print("Uppercase:", sentence.upper())

# Printing in lowercase
print("Lowercase:", sentence.lower())

# Replacing spaces with underscores
print("Replaced:", sentence.replace(" ", "_"))

# Removing leading and trailing spaces
print("Stripped:", sentence.strip())
```

**3**

```python
# Asking the user for a string
text = input("Enter a string: ")

# Removing spaces from the string
text_without_spaces = text.replace(" ", "")

# Counting characters
count = len(text_without_spaces)

# Displaying the result
print("Number of characters (excluding spaces):", count)
```

**4**

```python
print("Hello")
print("\tWorld")
print("This is a backslash: \\")
'''
\t → Inserts a tab space.
\\ → Prints a single backslash (\).
print() → Displays the text.
'''
```



