# 1. Input and Output in Python

## 1.1 Input from the User:

In Python, we use the `input()` function to take input from the user. The data entered by the user is always received as a string, so if you want to use it as a different data type (e.g., integer or float), you'll need to convert it using type conversion functions like `int()` or `float()`.

```python
name = input("Enter your name: ")
age = int(input("Enter your age: "))  # Convert input to integer
```

### Example:
```python
age=input("Enter Age: ")# it will take input from user and store in age variable and it will be in string format 
age=int(age)#we have to convert it into integer format using int() function and then we can perform any mathematical operation on it.
print("Age is: ", age)# output will be in integer format because we have converted it into integer format using int() function.
```

## 1.2 Output to the Console:

The `print()` function is used to display output to the console. You can use it to display text, variables, or results of expressions.

```python
The print() function is used to display output to the console. You can use it to display text, variables, or results of expressions.
```

You can also use f-strings (formatted string literals) for more readable code:

```python
print(f"Hello, {name}! You are {age} years old.")
```

### Example:

```python
boy_name = input("Enter the boy's name: ")# This line prompts the user to enter the boy's name and stores it in the variable 'boy_name'.
girl_name = input("Enter the girl's name: ")# This line prompts the user to enter the girl's name and stores it in the variable 'girl_name'.
print(boy_name +" loves "+ girl_name)# This line prints a message that combines the boy's name and the girl's name, indicating that the boy loves the girl.
```

## 1.3 Formatted String

```python
boy_name=input("Enter the boy name:")# This line prompts the user to enter the boy's name
boy_age=int(input("Enter the boy's age: "))# This line prompts the user to enter the boy's age and stores it as an integer because input() returns a string by default so we need to convert it to an integer using int() function
girl_name=input("Enter the girl name:")# This line prompts the user to enter the girl's name
girl_age=int(input("Enter the girl's age: "))# This line prompts the user to enter the girl's age and stores it as an integer because input() returns a string by default so we need to convert it to an integer using int() function
age_diff=boy_age-girl_age# This line calculates the age difference between the boy and the girl
age_diff=abs(age_diff)# This line takes the absolute value of the age difference to ensure that it is always a positive number, regardless of whether the boy is older or younger than
print(f"{boy_name} loves {girl_name}. Age difference: {age_diff}")# This line prints a message that combines the names and the age difference
print(boy_name + " loves " + girl_name + ". Age difference: " + str(age_diff))# This line prints a message that includes the boy and girl names and their age difference... The age difference is converted to a string using str() function so that it can be concatenated with the other strings in the print statement.
```

## 1.4 Absolute value function

```python
age_diff=abs(age_diff)# This line takes the absolute value of the age difference to ensure that it is always a positive number, regardless of whether the boy is older or younger than
```

# 3. Comments in Python

Comments are ignored by the Python interpreter and are used to explain the code or leave notes for yourself or others. They do not affect the execution of the program.

- **Single-line comments** start with `#`:

```python
# This is a single-line comment
print("Hello, World!")
```

- **Multi-line comments** can be written using triple quotes (`"""` or `'''`). These are often used to write detailed explanations or temporarily block sections of code:

```python
"""
This is a multi-line comment.
It can span multiple lines.
"""
print("Hello, Python!")
```
  

